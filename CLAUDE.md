# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository is currently empty. No source code, configuration, or documentation has been added yet.

Once the project is initialized (e.g., `git init`, framework scaffolding, or files copied in), update this file with:

1. **Commands** — build, lint, test, and run instructions (including how to run a single test).
2. **Architecture** — the high-level structure and how major components interact, focused on things that aren't obvious from browsing individual files.

Re-run `/init` after the codebase has real content so this file can be generated accurately.
